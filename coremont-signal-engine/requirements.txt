# Coremont Signal Engine — runtime dependencies
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
httpx>=0.27
SQLAlchemy>=2.0
python-dateutil>=2.9
python-multipart>=0.0.9   # form posts for export actions

# dev / test
pytest>=8.0

# Postgres driver — NOT needed for the default SQLite run. Install only when you
# point DATABASE_URL at Postgres:
#   pip install psycopg2-binary

